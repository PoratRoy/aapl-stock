import Tabs from "@/components/Tabs";
import styles from "./page.module.css";

const Home = () => {
    return (
        <main className={styles.main}>
            <Tabs />
        </main>
    );
};

export default Home;
